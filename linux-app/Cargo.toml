[package]
name = "linux-app"
version = "0.1.0"
edition = "2021"

[dependencies]
shared_core = { path = "../core" }
tokio = { version = "1.38", features = ["full"] }
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
